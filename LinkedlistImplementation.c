struct Node {
int data;
struct Node * next;
}
void pushItem(struct Node* ll, int item) {
    struct Node* ll = (struct Node*)malloc(sizeof(struct Node));
    return ll;
}
void main() {
    struct Node* ll = NULL;

}
